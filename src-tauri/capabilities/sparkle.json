{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sparkle",
  "description": "Sparkle auto-updates — macOS direct-download builds only",
  "platforms": ["macOS"],
  "windows": [
    "main"
  ],
  "permissions": [
    "sparkle-updater:default"
  ]
}
